import {BrowserRouter as Router, Route, Routes} from "react-router-dom";
export const AppRoutes = () => {
    return(
        <Router>
            <Routes>
                <Route>
                    <Router path={"*"} element={} />
                </Route>
            </Routes>
        </Router>
    );
}